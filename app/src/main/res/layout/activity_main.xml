<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/text_title"
        android:id="@+id/text_title"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_tab"
        android:id="@+id/btn_tab"
        android:onClick="onClickButtonTab"
        android:elevation="3dp"
        android:background="@color/accent_color"
        android:textAllCaps="false"
        android:textColor="@color/text_button"
        android:textSize="20sp"
        android:padding="5dp"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/text_title"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_sliding"
        android:id="@+id/btn_sliding"
        android:onClick="onClickButtonSliding"
        android:background="@color/accent_color"
        android:textAllCaps="false"
        android:textColor="@color/text_button"
        android:textSize="20sp"
        android:padding="5dp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_swipe"
        android:id="@+id/btn_swipe"
        android:onClick="onClickButtonSwipe"
        android:elevation="3dp"
        android:background="@color/accent_color"
        android:textAllCaps="false"
        android:textColor="@color/text_button"
        android:textSize="20sp"
        android:padding="5dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="90dp" />

</RelativeLayout>
